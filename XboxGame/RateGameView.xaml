<UserControl x:Class="XboxGame.RateGameView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:cal="http://www.caliburnproject.org"
             xmlns:local="clr-namespace:XboxGame"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Height="300" Width="800">
        <Label Content="{Binding Game.Title}" HorizontalAlignment="Left" Margin="334,32,0,0" FontWeight="Bold" VerticalAlignment="Top"/>
        <Button Content="Add Review" HorizontalAlignment="Left" Margin="582,216,0,0" VerticalAlignment="Top" Width="75"
                cal:Message.Attach="[Event Click]=[Action RateGame()]"/>
        <TextBox HorizontalAlignment="Left" Height="64" Margin="80,115,0,0" TextWrapping="Wrap" Text="{Binding ReviewComments}" VerticalAlignment="Top" Width="577"/>
        <ComboBox HorizontalAlignment="Left" Margin="80,64,0,0" VerticalAlignment="Top" Width="120"
                  ItemsSource="{Binding GameRatings}"  DisplayMemberPath="Id"
                  SelectedItem="{Binding SelectedGameRating}" />

    </Grid>
</UserControl>
